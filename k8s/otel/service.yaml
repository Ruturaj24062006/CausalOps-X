apiVersion: v1
kind: Service
metadata:
  name: otel-collector
  namespace: causalops
spec:
  type: ClusterIP
  ports:
    - port: 4317
      targetPort: 4317
      name: grpc
    - port: 4318
      targetPort: 4318
      name: http
  selector:
    app: otel-collector
