apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: causalops
spec:
  type: ClusterIP
  ports:
    - port: 9092
      targetPort: 9092
      name: client
    - port: 9093
      targetPort: 9093
      name: controller
  selector:
    app: kafka
